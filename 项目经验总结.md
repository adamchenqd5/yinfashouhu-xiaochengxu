# 银发守护者 - 项目开发经验总结

> 记录从需求到上线的完整过程，以及踩过的坑和经验教训。
> 最后更新：2026-06-27

---

## 一、项目背景

**目标用户**：老年人
**产品形态**：微信小程序（防诈骗聊天模拟器）
**核心功能**：通过模拟微信聊天对话，让老年人识别常见诈骗手段
**技术栈**：uni-app (Vue 3) → 微信小程序

---

## 二、关键踩坑记录

### 🔴 坑1：AppID 类型选错（最严重，浪费最多时间）

**现象**：微信开发者工具无论怎么配置，预览时始终报错 `800059: /game.json not found`，且自动切换到"小游戏模式"。

**根因**：注册微信小程序账号时，误选了**"小游戏"**类型，导致该 AppID 在微信服务器端被标记为小游戏。开发者工具编译时会向服务器查询 AppID 类型，服务器返回"小游戏"，工具就强制按小游戏模式编译，忽略 `project.config.json` 里的 `compileType: "miniprogram"`。

**解决方案**：
- **预防**：注册账号时务必选"小程序"，不要选"小游戏"
- **修复**：重新注册一个小程序类型的账号，换新的 AppID
- **注意**：小游戏 AppID 和小程序 AppID 是两种不同账号，**不能互相转换**

**经验教训**：
> ⚠️ AppID 类型问题无法通过任何本地配置解决，必须在注册时就选对。

---

### 🔴 坑2：pages/game 目录名触发误识别

**现象**：开发者工具看到 `pages/game/game.json` 文件，自动将项目识别为小游戏。

**解决方案**：将 `pages/game/` 重命名为 `pages/chat/`，避免目录名包含 `game` 关键词。

**经验教训**：
> ⚠️ 微信开发者工具有自动识别逻辑，页面目录名不要包含 `game`、`minigame` 等关键词。

---

### 🔴 坑3：project.config.json 被开发者工具自动篡改

**现象**：手动修改 `project.config.json` 后，用开发者工具打开项目，文件被自动添加 `condition.game` 字段。

**根因**：开发者工具会在项目目录下写入 `project.private.config.json`，这个文件的优先级高于 `project.config.json`，且会缓存项目类型。

**解决方案**：
- 提供正确的 `project.private.config.json`，覆盖工具的缓存配置
- 或者让用户手动删除 `project.private.config.json` 后重新导入

**经验教训**：
> ⚠️ `project.private.config.json` 优先级 > `project.config.json`，分发项目时要包含正确的 private config。

---

### 🟡 坑4：uni-app 构建会覆盖 project.config.json

**现象**：每次运行 `uni build -p mp-weixin`，`project.config.json` 会被重新生成，之前的手动修改丢失。

**解决方案**：构建完成后，用脚本自动覆盖正确的 `project.config.json`。

**经验教训**：
> ⚠️ uni-app 的 `project.config.json` 每次构建都会重新生成，不能直接手动修改构建输出里的文件，要在构建后自动替换。

---

### 🟡 坑5：GitHub 推送时 token 失效

**现象**：`git push` 报错 `Authentication failed`，token 过期。

**解决方案**：使用 `gh auth token` 获取当前有效 token，或通过 `gh` CLI 直接操作。

---

## 三、防诈骗小程序内容设计经验

### 场景设计原则
1. **真实感**：用老年人熟悉的微信聊天界面，骗子头像用真实感的表情/名字
2. **渐进式难度**：先简单后复杂，第一个场景用"保健品诈骗"（最常见）
3. **及时反馈**：选错选项后立即显示"❌ 这是诈骗！"，并解释原因

### 老年人 UX 设计原则
1. **字体放大**：所有文字至少 32rpx，关键按钮文字 36rpx
2. **对比度高**：文字和背景对比要强，不要用浅灰字
3. **按钮要大**：选项按钮高度至少 80rpx，方便手指点击
4. **反馈明确**：选对/选错要有明显的颜色区分（绿/红）
5. **避免误触**：按钮之间留足够间距

---

## 四、微信小程序提交流程

1. 开发者工具点 **"上传"**，填写版本号和说明
2. 登录 **mp.weixin.qq.com**
3. **开发管理 → 开发版本** → 看到上传的版本
4. 先 **"设为体验版"**（可选，内部测试用）
5. 点 **"提交审核"**，填写服务类目和标签
6. 审核通过后点 **"发布"**

**审核周期**：一般 1-3 天

**服务类目建议**：教育-科普 / 健康医疗-健康科普

---

## 五、项目文件结构

```
src/
  pages/
    index/     - 首页（开始页面）
    select/    - 场景选择页
    chat/      - 聊天对话页（游戏核心，注意目录名不要用 game）
  data/
    scenes.js  - 诈骗场景数据（6个场景）
    rounds.js  - 对话回合数据（59轮对话）
  App.vue
  main.js
  manifest.json   - AppID 配置在这里
  pages.json      - 页面路由配置
```

---

## 六、AppID 记录

| 用途 | AppID | 状态 |
|------|-------|------|
| 错误的小游戏账号 | wx12caf2c75b485ad2 | ❌ 已废弃（类型是小游戏） |
| 正式小程序账号 | wx03b9dcb19055f27d | ✅ 使用中 |

---

## 七、待优化项

1. **UI**：底部选项按钮太宽，遮挡聊天文字（用户反馈）
2. **内容**：目前只有 6 个场景，可继续扩充
3. **功能**：增加"学习报告"，展示用户识别准确率
4. **分享**：增加微信分享功能，让更多老年人看到

---

*本文档随项目开发持续更新*
